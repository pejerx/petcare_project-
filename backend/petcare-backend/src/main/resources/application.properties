# Application Name
spring.application.name=backend

# Database Configuration (PostgreSQL - Neon)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-falling-violet-aisscdsu-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_0cZKwE3bhjSI}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate & JPA Config
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging & Error Settings
server.error.include-stacktrace=never
debug=true

# CORS (set CORS_ALLOWED_ORIGINS on Render with your frontend URL)
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
